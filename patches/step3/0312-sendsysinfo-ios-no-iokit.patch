diff --git a/src/slic3r/GUI/SendSystemInfoDialog.cpp b/src/slic3r/GUI/SendSystemInfoDialog.cpp
index ed22a26..fa3a46f 100644
--- a/src/slic3r/GUI/SendSystemInfoDialog.cpp
+++ b/src/slic3r/GUI/SendSystemInfoDialog.cpp
@@ -1,8 +1,11 @@
 #include "SendSystemInfoDialog.hpp"
 
 #if __APPLE__
+#include <TargetConditionals.h>
+#if TARGET_OS_OSX
 #import <IOKit/IOKitLib.h>
 #endif
+#endif
 
 #include "libslic3r/AppConfig.hpp"
 #include "libslic3r/BlacklistedLibraryCheck.hpp"
@@ -49,9 +52,12 @@
     //#include <atlbase.h>
     #include <Iphlpapi.h>
     #pragma comment(lib, "iphlpapi.lib")
-#elif __APPLE__
+#elif __APPLE__ && TARGET_OS_OSX
+    // Only the macOS IORegistry hardware-UUID code needs CoreFoundation; on
+    // iOS the include would drag MacTypes.h whose struct Rect collides with
+    // Slic3r::GUI::Rect.
     #include <CoreFoundation/CoreFoundation.h>
-#else // Linux/BSD
+#else // Linux/BSD (and iOS, which takes the machine-id branch)
     #include <charconv>
 #endif
 
@@ -323,7 +329,7 @@ static std::string get_unique_id()
             unique = macs.front();
     }
     free(AdapterInfo);
-#elif __APPLE__
+#elif __APPLE__ && TARGET_OS_OSX
     constexpr int buf_size = 100;
     char buf[buf_size] = "";
     memset(&buf, 0, sizeof(buf));
@@ -389,12 +395,14 @@ static std::string generate_system_info_json()
 
     // Get system language.
     std::string sys_language = "Unknown"; // important to init, see the __APPLE__ block.
-    #ifndef __APPLE__
-        // Following apparently does not work on macOS.
+    #if !defined(__APPLE__) || !TARGET_OS_OSX
+        // Following apparently does not work on macOS (fine on iOS, where the
+        // CFLocale path below is unusable: any CoreFoundation include drags
+        // MacTypes.h whose struct Rect collides with Slic3r::GUI::Rect).
         const wxLanguage lang_system = wxLanguage(wxLocale::GetSystemLanguage());
         if (lang_system != wxLANGUAGE_UNKNOWN)
             sys_language = wxLocale::GetLanguageInfo(lang_system)->CanonicalName.ToUTF8().data();
-    #else // __APPLE__
+    #else // macOS
         CFLocaleRef cflocale = CFLocaleCopyCurrent();
         CFStringRef value = (CFStringRef)CFLocaleGetValue(cflocale, kCFLocaleLanguageCode);
         char temp[10] = "";
