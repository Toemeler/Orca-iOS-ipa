diff --git a/src/libvgcode/glad/src/gles2.c b/src/libvgcode/glad/src/gles2.c
index e9642e9..bc59131 100644
--- a/src/libvgcode/glad/src/gles2.c
+++ b/src/libvgcode/glad/src/gles2.c
@@ -761,6 +761,10 @@ static GLADapiproc glad_dlsym_handle(void* handle, const char *name) {
   typedef __eglMustCastToProperFunctionPointerType (GLAD_API_PTR *PFNEGLGETPROCADDRESSPROC)(const char *name);
 #endif
   extern __eglMustCastToProperFunctionPointerType emscripten_GetProcAddress(const char *name);
+#elif defined(__APPLE__)
+  /* iOS/macOS: GLES linked directly from OpenGLES framework; no EGL. (Orca-iOS-ipa) */
+  typedef void (*__eglMustCastToProperFunctionPointerType)(void);
+  typedef __eglMustCastToProperFunctionPointerType (*PFNEGLGETPROCADDRESSPROC)(const char *name);
 #elif GLAD_GLES2_USE_SYSTEM_EGL
   #include <EGL/egl.h>
   typedef __eglMustCastToProperFunctionPointerType (GLAD_API_PTR *PFNEGLGETPROCADDRESSPROC)(const char *name);
@@ -827,6 +831,10 @@ static struct _glad_gles2_userptr glad_gles2_build_userptr(void *handle) {
     return userptr;
 }
 
+#if defined(__APPLE__)
+int gladLoaderLoadGLES2(void) { return 1; }
+void gladLoaderUnloadGLES2(void) { }
+#else
 int gladLoaderLoadGLES2(void) {
     int version = 0;
     void *handle = NULL;
@@ -863,6 +871,8 @@ int gladLoaderLoadGLES2(void) {
 
 
 
+#endif /* __APPLE__ */
+
 void gladLoaderUnloadGLES2(void) {
     if (_glad_GLES2_loader_handle != NULL) {
         glad_close_dlopen_handle(_glad_GLES2_loader_handle);
