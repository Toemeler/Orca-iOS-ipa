diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8dcd4cf..5a05c56 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -154,6 +154,11 @@ if (APPLE)
     if (CMAKE_OSX_ARCHITECTURES AND _arch_idx LESS 0)
         set(IS_CROSS_COMPILE TRUE)
     endif ()
+    if (CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        # Host is macOS, target is iOS: always a cross build even on same arch,
+        # so host-run build helpers (encoding-check) must be disabled.
+        set(IS_CROSS_COMPILE TRUE)
+    endif ()
     if (CMAKE_MACOSX_BUNDLE)
         set(CMAKE_INSTALL_RPATH @executable_path/../Frameworks)
     endif()
@@ -688,14 +693,20 @@ endif ()
 
 find_package(PNG REQUIRED)
 
-set(OpenGL_GL_PREFERENCE "LEGACY")
-find_package(OpenGL REQUIRED)
-
-if(APPLE AND CMAKE_VERSION VERSION_GREATER_EQUAL "4.0")
-    set(OPENGL_LIBRARIES "-framework OpenGL" CACHE STRING "OpenGL framework" FORCE)
-endif()
-
-find_package(glfw3 REQUIRED)
+if (SLIC3R_GUI)
+    set(OpenGL_GL_PREFERENCE "LEGACY")
+    if (CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        # iOS ships OpenGL ES (EAGL); desktop FindOpenGL and GLFW do not apply.
+        set(OPENGL_FOUND TRUE)
+        set(OPENGL_LIBRARIES "-framework OpenGLES" CACHE STRING "OpenGL ES framework" FORCE)
+    else ()
+        find_package(OpenGL REQUIRED)
+        if(APPLE AND CMAKE_VERSION VERSION_GREATER_EQUAL "4.0")
+            set(OPENGL_LIBRARIES "-framework OpenGL" CACHE STRING "OpenGL framework" FORCE)
+        endif()
+        find_package(glfw3 REQUIRED)
+    endif ()
+endif ()
 
 # Find the Cereal serialization library
 find_package(cereal REQUIRED)
