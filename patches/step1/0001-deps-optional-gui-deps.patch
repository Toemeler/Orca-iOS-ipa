diff --git a/deps/CMakeLists.txt b/deps/CMakeLists.txt
index 26a1860..897163d 100644
--- a/deps/CMakeLists.txt
+++ b/deps/CMakeLists.txt
@@ -54,6 +54,7 @@ if (NPROC EQUAL 0)
 endif ()
 
 set(DEP_DOWNLOAD_DIR ${CMAKE_CURRENT_SOURCE_DIR}/DL_CACHE CACHE PATH "Path for downloaded source packages.")
+option(ORCA_DEPS_GUI "Build GUI-only dependencies (GLEW, GLFW, OpenCSG, wxWidgets)" ON)
 set(FLATPAK FALSE CACHE BOOL "Toggles various build settings for flatpak, like /usr/local in DESTDIR or not building wxwidgets")
 
 if ("${DESTDIR}" STREQUAL "" OR "${DESTDIR}" STREQUAL "${AUTOGENERATED_DESTDIR}")
@@ -346,10 +347,11 @@ include(Boost/Boost.cmake)
 
 include(Cereal/Cereal.cmake)
 include(Qhull/Qhull.cmake)
-include(GLEW/GLEW.cmake)
-
-include(GLFW/GLFW.cmake)
-include(OpenCSG/OpenCSG.cmake)
+if (ORCA_DEPS_GUI)
+    include(GLEW/GLEW.cmake)
+    include(GLFW/GLFW.cmake)
+    include(OpenCSG/OpenCSG.cmake)
+endif()
 
 include(TBB/TBB.cmake)
 
@@ -395,9 +397,11 @@ endif()
 
 # flatpak builds wxwidgets separately, so it is not included in the deps target
 set(WXWIDGETS_PKG "")
-include(wxWidgets/wxWidgets.cmake)
-if (NOT FLATPAK)
-    set(WXWIDGETS_PKG "dep_wxWidgets")
+if (ORCA_DEPS_GUI)
+    include(wxWidgets/wxWidgets.cmake)
+    if (NOT FLATPAK)
+        set(WXWIDGETS_PKG "dep_wxWidgets")
+    endif()
 endif()
 
 set(FREETYPE_PKG "")
@@ -423,6 +427,13 @@ endif ()
 include(OCCT/OCCT.cmake)
 include(OpenCV/OpenCV.cmake)
 
+set(OPENCSG_PKG "")
+set(GLFW_PKG "")
+if (ORCA_DEPS_GUI)
+    set(OPENCSG_PKG dep_OpenCSG)
+    set(GLFW_PKG dep_GLFW)
+endif()
+
 set(_dep_list
     dep_Boost
     dep_TBB
@@ -433,11 +444,11 @@ set(_dep_list
     dep_Draco
     dep_NLopt
     dep_OpenVDB
-    dep_OpenCSG
+    ${OPENCSG_PKG}
     dep_OpenCV
     dep_Eigen
     dep_CGAL
-    dep_GLFW
+    ${GLFW_PKG}
     dep_OCCT
     ${FREETYPE_PKG}
     ${PNG_PKG}
