# Patches

Ordered `NNNN-description.patch` files, applied with `git apply` by each stage's
workflow against the pinned upstream ref. One patch per subsystem; every patch header
must state why it exists and which stage failure it fixes. All patches AGPL-3.0.
